# PixVerse Python SDK for RunAPI

Install `runapi-pixverse` and use `PixVerseClient` for all five PixVerse V6 video resources.

```bash
pip install runapi-pixverse
```

Model details and pricing: https://runapi.ai/models/pixverse/pixverse-v6
